#include <stdio.h>

int main() {
    int length, breadth;

    scanf("%d", &length);
    scanf("%d", &breadth);

    int rope_length = 2 * (length + breadth);
    int carpet_quantity = length * breadth;

    printf("%d\n", rope_length);
    printf("%d\n", carpet_quantity);

    return 0;
}
